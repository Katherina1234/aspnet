@model lab1_1.Models.Timetable
@{
    ViewBag.Title = "Добавление пользователя";
}
<form asp-action="create" asp-controller="home">
    <div class="form-group">
        <label asp-for="train_num" class="control-label">Имя</label>
        <input type="text" asp-for="train_num" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="prub" class="control-label">Возраст</label>
        <input type="number" asp-for="prub" class="form-control" />
    </div>
    <div class="form-group">
        <input type="submit" value="Отправить" class="btn btn-default" />
    </div>
</form>